## scripts simples para rodar no seu PC
│   ├── build.sh    → build inicial (copiar arquivos, minificação futura)
│   ├── deploy.sh   → deploy inicial (ex: GitHub Pages)
│   └── utils.sh    → utilitários (ex: criar nova pasta de site)